//Masterlists

var masterList = {};


var mutators = [];
for (var key in mutate){
  mutators.push(key);
}
var emptyObj = {};
for (var key in emptyObj){
  var i = mutators.indexOf(key);
  if (if != -1) {
    array.splice(i, 1);
  }
}

console.log(mutators);


for(var i=32;i<32+mutators.length;++i) 
  console.log(String.fromCharCode(i) 


